---- Abstract Factory :

--definition:
"Similar to the AbstractFactory, this pattern is used to create series of related or dependent objects.
The difference between this and the abstract factory pattern is that the static factory pattern uses just one
static method to create all types of objects it can create."

"create list of related object with only static  function for all classes only one instance"


-- Examples
1- create car using static factory